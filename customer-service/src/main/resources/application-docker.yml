server:
  port: ${PORT:0}
  servlet:
    context-path: /api/v1


spring:
  application:
    name: customer-service

  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://localhost:5432/customerDB
    username: sewah
    password: password
    driverClassName: org.postgresql.Driver
    sql:
      init:
        platform: postgres
  zipkin:
    base-url: ${ZIPKIN_BASE_URL:http://zipkin:9411}
  rabbitmq:
    addresses: localhost:5672
#  datasource:
#    username: 'sewah'
#    url: jdbc:postgresql://localhost:5432/customerDB
#    password: 'password'
#    jpa:
#      properties:
#        hibernate:
#          dialect: org.hibernate.dialect.PostgreSQLDialect
#          format_sql: 'true'
#      hibernate:
#        ddl-auto: create-drop
#      show-sql: 'true'

eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_SERVER_URL:http://eureka_server:8761/eureka}
    fetch-registry: true
    register-with-eureka: true
